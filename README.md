This repo contains brief python code for simulating double pendulums with GPU acceleration.

The scripts uses JAX and Diffrax with 64-bit precision enabled. Outputs are located in the `outputs/` directory. `data_*.npz` contains the raw Lyapunov exponents. `*.png` images contain the rendered images of the data.

Images generated by this code and included in this repo are released under CC0.
